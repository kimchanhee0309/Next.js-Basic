#### netx/image
* Next.js 내 내장된 이미지 처리 **서버와의 창구** 역할 수행
* **이미지를 최적화해주는 고급 컴포넌트**
* 내부적으로는 **이미지 요청을 Next.js 서버로 보내고**, 그 서버가 이미지 리사이징, 포맥 변환(WebP 등), Lazy Loading, 레이아웃 시프트 방지 등을 한 뒤에 최종 이미지를 응답해줌
* So, 앞으로는 `<img />` 태그 대신 항상 `<Image />` 태그 쓰기

#### Image 컴포넌트 내용
* `src`(이미지 경로), `alt`(대체 텍스트)는 필수 속성
* `width`, `height`: 지정(px 단위) 시 `해당 비율`에 맞춰 최적 이미지 사이즈로 변형(resizing)
* width, height 속성은 정적 이미지의 비율을 알고 있을 때 적합
```JavaScript
import Image from 'next/image'

export default function Page() {
  return (
    <div>
      <Image
        src="/profile.png"
        width={500}
        height={500}
        alt="Picture of the author"
      />
    </div>
  )
}
```
* **희망 렌더링 사이즈 지정**:
  * 브라우저에게 "이 이미지를 이 크기로 표시하고 싶어요"라고 알려줌
  * 희망 렌더사이즈는 실제 렌더사이즈가 아니므로 CSS(className)으로 크기 지정이 필요할 수 있음
* **최적화를 위한 참고 정보**:
  * Next.js가 이미지를 최적화할 때 이 크기를 참고하여 적절한 크기의 이미지를 생성함
* **레이아웃 시프트 방지(CLS 방지)를 위한 공간 예약**:
  * 이미지가 로드되기 전에 브라우저에게 "이 크기만큼 공간을 미리 확보해두세요"라고 알려줌
  * 이미지가 로드되면서 페이지 레이아웃이 갑자기 변경되는 문제를 방지함
  * height 속성 중요
* **플레이스홀더(placeholder) 사이즈 결정**:
  * `placeholder="blur"` 또는 `placeholder="empty"` 옵션을 사용할 때 placeholder의 크기는 이 `width`와 `height` 값을 기반으로 함
  * 로딩 중에 표시되는 임시 공간의 크기를 결정함
 
<br>

외부 이미지를 Image 컴포넌트 src에 할당하고자 할 때
`next.config.mjs`에 remotePattern에 설정해줘야 함
```JavaScript
/* @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'example.com',
        port: '',
        pathname: '/account123/**',
        search: '',
      },
    ],
  },
};

export default nextConfig;
```

<br>

**`fill`**: 부모요소의 크기에 따라 이미지 크기를 정하고자 할 때
(width, height 보다는 fill 방식에 익숙해지는 것을 권장)
```JavaScript
<figure className="relative w-full h-64">
  <Image
    src="/example.jpg"
    alt="풍경 사진"
    fill
    sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"
    className="object-cover"
  />
</figure>
```
* fill 사용 시 width, height 속성을 사용하지 않음
* fill 속성을 쓰면 부모 요소(figure)의 크기에 이미지 사이즈가 결정됨
* fill 속성을 사용하기 위해서는 부모 요소는 position relative 설정이 필요함
* object-cover(tailwind utility class)로 원본 이미지 비율을 유지 하면서 부모 요소 크기에 따라 사이지를 늘림
* fill 속성 사용 시 반드시 sizes 속성도 함께 써주는 것이 좋음. 그렇지 않으면 sizes는 기본값 100vw로 간주하여 원하는 방향으로 최적화가 동작하지 않음

<br>

**`placeholder`**: 이미지가 로드되는 동안 보여줄 기본 이미지 지정하고자 할 때
```JavaScript
import profilePic from '../public/profile.jpg'

function Profile() {
  return (
    <Image
      src={profilePic}
      placeholder="blur"
      alt="프로필 사진"
      width={500}
      height={500}
    />
  )
}
```
* 정적 이미지는 placeholder="blur" 설정만 해도 자동으로 blur 이미지 처리
* 동적(외부) 이미지는 placeholder="blur"과 blurDataURL 설정도 해줘야 함

```JavaScript
// 서버 컴포넌트나 getStaticProps 같은 서버 사이드 환경에서 사용
import { getPlaiceholder } from 'plaiceholder';
import Image from 'next/image';

// 서버 컴포넌트에서 사용하는 예시
async function MyImageComponent() {
  // 외부 이미지 URL
  const imageUrl = 'https://example.com/profile.jpg';

  // 이미지 데이터 가져오기
  const response = await fetch(imageUrl);
  const buffer = await response.arrayBuffer();

  // plaiceholder를 사용하여 blur 데이터 생성
  const { base64 } = await getPlaiceholder(Buffer.from(buffer));

  return (
    <div>
      <Image
        src={imageUrl}
        alt="프로필 이미지"
        width={500}
        height={300}
        placeholder="blur"
        blurDataURL={base64}
      />
    </div>
  );
}

export default MyImageComponent;
```
