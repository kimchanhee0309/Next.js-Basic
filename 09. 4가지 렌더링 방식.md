#### SSG → Static Rendering
* **Static Site Generation(정적 사이트 생성)** 은 빌드 시에 미리 페이지를 만들어놓고 사용자가 해당 라우트에 들어왔을 때 바로 화면에 보여줄 수 있는 html 파일을 클라이언트에 줘서 화면을 그리는 렌더링 방식
* 바뀌지 않는 정적인 컴포넌트에 적합한 렌더링 방식이며 Next.js `기본 렌더링 방식`임
* SSG 방식은 `빌드 시` 해당 컴포넌트를 `100% 프리렌더링` 해놓고 클라이언트 요청 시  ㅔ공함
* 참고 : 프리렌더링 된 결과는 Full Route Cache에 저장됨

___

#### ISR → Static Rendering with Revalidation
**ISR(Incremental Static Regeneration)이란?**
빌드 시 `100% 프리렌더링` 해놓고, 일정시간 지나면 사용자 요청에 따라 페이지 재생성할 준비하는 렌더링 방식

* 두 가지 ISR 적용 방법
  * **1. Route Segment Config**
    ```TypeScript
    // layout.jsx | page.jsx | route.js
    export const revalidate = 5; // 5초 간격의 ISRP
    ```
  * **fetch 옵션**
    ```TypeScript
    fetch('https://jsonplaceholder.typicode.com/todos/1', { next: { revalidate: 5 } })
    ```

___

#### SSR → Dynamic Rendering
사용자 요청마다 매번 서버 컴포넌트가 실행되면서 완성한 html을 브라우저에 제공함

* 4가지 SSR 적용 케이스
  * **1. Route Segment Config**
    ```TypeScript
    // layout.tsx | page.tsx | route.ts
    ```
    
  * **2. fetch 옵션**
    ```TypeScript
    fetch('https://jsonplaceholder.typicode.com/todos/1', { cache: 'no-store' })
    ```
    
  * **3. Dynamic Routing을 적용한 경우**
    * 동적 라우팅인 경우, 기본 렌더링 방식이 SSG가 아닌 SSR이 됨
      
  * **4. Dynamic Functions를 사용한 경우**
    **(1) `cookies()**
    ```TypeScript
    import { cookies } from 'next/headers'

    export default async function Page() {
      const cookieStore = await cookies()
      const theme = cookieStore.get('theme')
      return '...'
    }
    ```
    
    **(2) `headers()**
    ```TypeScript
    import { headers } from 'next/headers';

    export default async function Page() {
      // 현재 요청의 헤더 가져오기
      const headers.List = await headers();

      // 특정 헤더 값 가져오기 (User-Agent 예시)
      const userAgent = headersList.get('user-agent');

      return (
        <div>
          <h1>Dynamic Headers Example</h1>
          <p>Your User-Agent is:</p>
          <pre>{userAgent}</pre>
        </div>
      );
    }
    ```

___

#### CSR → Client Components
컴포넌트 최상단에 **`"use client"`**를 넣어주면 브라우저에서 동작할 수 있는 클라이언트 컴포넌트로 인식함
```TypeScript
"use client"
```

___

#### hydration이란?
프리렌더링된 html이 클라이언트에서 JavaScript를 통해 인터랙티브하게 변환되는 과정
