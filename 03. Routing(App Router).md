# 1. Routing 이란? 

<details>
  <summary>1-1. 라우팅의 의미</summary>

  |구분|라우팅의 의미|
  |---|---|
  |**백엔드 라우팅**|클라이언트가 요청한 URL에 대해, 서버 내 **어떤 핸들러가 그 요청을 담당할지 결정**하는 것(예: /api/users 요청 시 getUsers 실행)|
  |**프론트엔드 라우팅**|사용자가 URL을 바꿨을 때, **어떤 화면(컴포넌트)을 보여줄지** 정하는 것(예: /about에서 About 페이지 렌더링)|

</details>

<details>
  <summary>1-2. URL 구조</summary>

  **app/** 아래에서의 폴더명 url path로 동작(단, page.jsx, route.jsx 파일이 존재할 경우만 해당)
  
</details>

<details>
  <summary>1-3. 서버 컴포넌트 vs. 클라이언트 컴포넌트</summary>

  #### 서버 컴포넌트
  * App Router 기본 컴포넌트는 서버 컴포넌트
  * 서버에서 HTML로 그려진 후 클라이언트로 전송
  * 컴포넌트를 async 함수로 제작 가능
  * 브라우저 API 사용 못함
  * hook 사용 못함(useState X)

  #### 클라이언트 컴포넌트
  * "use client"로 선언 필요
  * 브라우저 API 사용 가능
  * 프리렌더링 마찬가지로 적용됨
  * hook 사용 가능(useState O)
  * JavaScript가 로드된 후 하이드레이션 과정을 통해 인터랙티브하게 됨
</details>

<details>
  <summary>1-4. layout</summary>

  같은 디렉터리 내에 `layout.jsx`가 있으면 같은 디렉터리에 있는 page.jsx 또는 그 하위에 있는 page.jsx들에게 모두 레이아웃을 적용함

  ```JavaScript
  // 일반 React 방식의 Layout 컴포넌트
  function Layout({ children }) {
    return (
      <div className="layout">
        <Header />
        <main>
          {children}
        </main>
        <Footer />
      </div>
    );
  }

  // Page.jsx
  function Page() {
    return (
      <Layout>
        <h1>페이지 제목</h1>
        <p>페이지 내용이 여기에 들어갑니다.</p>
      </Layout>
    );
  }
  ```
</details>

<details>
  <summary>1-5. Link 컴포넌트</summary>

  `client-side navigation` 방식으로 페이지 이동
  
  ```JavaScript
  // Link 클릭 시 클라이언트 사이드 라우팅합니다.
  <Link href="/detail" />
  ```

  * 클릭 시 `단순 페이지 이동이 필요할 때`는 Link 컴포넌트를 사용
  * Next.js에서는
    * 초기 페이지 로드 시에는 **server-side navigation**으로 동작
    * 이후 페이지 전환 시에는 **client-side navigation**으로 동작하는 하이브리드 네비에기션 방식으로 동작

  ```txt
  client side navigation?
  - 페이지를 이동할 때, 전체 페이지를 다시 불러오지 않고, 브라우저가 JavaSCript로 필요한 데이터나 컴포넌트만 바꿔서 화면을 갱신하는 방식
  - 특징
    1. 빠른 전환 속도
    2. 부드러운 UX
    3. 브라우저 요청 감소
  ```
  ```txt
  server side navigation?
  - 링크 클릭 → 서버에 요청 → 전체 HTML을 새로 받아옴 → 브라우저 전체 새로고침
  - 전통적인 PHP, JSP, Ruby on Rails 기반 웹사이트나 Next.js에서 <a href="..."/>로 완전한 이동 시
  - 초기 구현이 단순함, SEO 친화적(특히 JS 없이도 작동)
  - 매 이동마다 새로고침 → UX 느림
  ```
</details>

<details>
  <summary>1-6. useRouter</summary>

  ```TypeScript
  'use client'

  import { useRouter } from 'nex/navigation'

  export default function Page() {
    const router = useRouter()

    const handleClick = () => {
      // 뭔가 처리 이후 페이지 이동 api 요청 이후에 페이지 이동
      router.push('/dashboard')
    }

    return (
      <button type="button" onClick={handlerClick}>
        Dashboard
      </button>
    )
  }
  ```
  * 클릭 시 페이지 `이동 전에 로직 수행이 필요할 때` useRouter를 사용함
  * useRouter는 반드시 **'next/navigation'** 에서 import 해야 함
  
</details>

<details>
  <summary>1-7. Link vs useRouter</summary>

  |상황|Link 가능?|useRouter 가능?|
  |---|---|---|
  |유저 클릭으로 페이지 이동|O|O|
  |API 성공 시 자동 페이지 이동|X|O|
  |프로그래틱 리디렉션|X|O|
  |SEO(HTML <a>처럼 인식됨)|O|X|
  |자동 프리패치(빠른 UX)|O|X|
</details>
